# PTS - Test/geliştirme bağımlılıkları
# Kurulum: pip install -r backend/requirements.txt -r backend/requirements-dev.txt
# Testleri çalıştırma: proje kökünden `pytest`

pytest==8.3.3
httpx==0.28.1          # FastAPI TestClient bunu kullanır (starlette >= 0.36)

# pdf_export.py testlerinde ÜRETİLEN PDF'in içindeki metni gerçekten geri
# okuyup Türkçe karakterlerin (ı/İ/ş/Ş/ğ/Ğ) doğru göründüğünü doğrulamak
# için (bkz. tests/test_pdf_export.py, 2026-09-18 font düzeltmesi).
pdfplumber==0.11.9

# camera_reader.py testleri için (ağır fast-alpr/onnx modeline gerek yok,
# testler sahte/basit bir ANPR motoru kullanır — sadece opencv + requests yeterli)
opencv-python-headless==4.10.0.84
requests==2.32.3
