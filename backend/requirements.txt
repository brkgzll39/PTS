# PTS - Plaka Tanıma Sistemi - Temel Bağımlılıklar
# Kurulum: pip install -r requirements.txt
#
# ÖNEMLİ: Python 3.12 ile test edilip sabitlendi. Çok daha yeni bir Python
# sürümünde (ör. 3.13, 3.14) Pillow/pydantic gibi paketlerin Windows için
# henüz hazır kurulum dosyası (wheel) olmayabilir; pip kaynağından derlemeye
# çalışır ve genellikle zlib/Rust derleyicisi eksik olduğu için başarısız
# olur (2026-09-26, gerçek kurulumda karşılaşılan hata -- bkz. README.md'deki
# "Manuel Kurulum" bölümü). `kurulum.bat` (Windows) bunu `uv --python 3.12`
# ile otomatik yönetir; elle kurulumda `py -3.12 -m venv .venv` kullanın.

fastapi==0.115.0
uvicorn[standard]==0.53.0
sqlalchemy==2.0.35
pyodbc==5.2.0
pydantic==2.9.2
python-multipart==0.0.9
openpyxl==3.1.5
reportlab==4.2.5
# "GEÇİŞ RAPORU" PDF'indeki araç görsellerini gömmeden önce küçültüp yeniden
# sıkıştırmak için (bkz. backend/pdf_export.py::_kucuk_gorsel_akisi) --
# reportlab'ın Image flowable'ı JPEG kodlamak için zaten dolaylı olarak
# Pillow'a ihtiyaç duyar, burada doğrudan/açık bir bağımlılık olarak listelendi.
Pillow==11.0.0
# Kurumsal ağlarda (ör. TPAO) dış HTTPS trafiğini (Telegram, webhook)
# bir SSL inceleme/proxy cihazından geçiren kurulumlarda Python'ın
# kendi (OpenSSL/certifi) sertifika listesi yerine İŞLETİM SİSTEMİNİN
# (Windows'un) zaten güvendiği sertifika deposunu kullanmasını sağlar --
# bkz. backend/main.py'nin en üstündeki `truststore.inject_into_ssl()`
# çağrısının kök neden notu ("CERTIFICATE_VERIFY_FAILED: self-signed
# certificate in certificate chain" gerçek üretim hatası, 2026-09-26).
# NOT: diğer bağımlılıkların aksine tam sürüm (==) yerine alt sınır (>=)
# ile belirtildi -- küçük, tek amaçlı ve seyrek güncellenen bir paket;
# kurulum anındaki en güncel sürümün alınması burada riskli değil.
truststore>=0.8

# -----------------------------------------------------------------
# OPSİYONEL: Gerçek ANPR/RTSP kamera pipeline (backend/camera_reader.py)
# kullanmak isterseniz aşağıdakileri ayrıca kurun:
#
#   pip install "fast-alpr[onnx]" opencv-python requests
# Windows DirectML (GPU) alternative:
#   pip install "fast-alpr[onnx-directml]" opencv-python requests
#
# OPSİYONEL: LED paneli seri port (RS232/USB) üzerinden bağlıyorsanız:
#
#   pip install pyserial
# -----------------------------------------------------------------
